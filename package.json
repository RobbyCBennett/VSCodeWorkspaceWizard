{
	"name": "workspaceWizard",
	"displayName": "Workspace Wizard",
	"description": "Easily create, organize, & switch workspaces",
	"version": "1.0.0",
	"publisher": "rob-bennett",
	"repository": {
		"type": "git",
		"url": "https://github.com/RobbyCBennett/VSCodeWorkspaceWizard.git"
	},
	"engines": {
		"vscode": "^1.77.1"
	},
	"categories": [
		"Other",
		"Keymaps",
		"Visualization"
	],
	"keywords": [
		"Switch",
		"Workspace",
		"Project",
		"Organize",
		"Open"
	],
	"icon": "icon.png",
	"main": "./extension.js",
	"contributes": {
		"configuration": {
			"title": "Workspace Wizard",
			"properties": {
				"workspaceWizard.general.folder": {
					"type": "string",
					"description": "Folder that contains .code-workspace files"
				},
				"workspaceWizard.quickPick.showOnBlankWindow": {
					"type": "boolean",
					"default": true,
					"description": "Show the Workspaces quick pick when a new blank window is opened"
				},
				"workspaceWizard.quickPick.showOnUsedWindow": {
					"type": "boolean",
					"default": false,
					"description": "Show the Workspaces quick pick when a used window is opened"
				},
				"workspaceWizard.sidebar.expand": {
					"type": "boolean",
					"default": true,
					"description": "Expand folders in the Workspaces sidebar"
				},
				"workspaceWizard.sidebar.showOnBlankWindow": {
					"type": "boolean",
					"default": true,
					"description": "Show the Workspaces sidebar when a new blank window is opened"
				},
				"workspaceWizard.sidebar.showOnUsedWindow": {
					"type": "boolean",
					"default": false,
					"description": "Show the Workspaces sidebar when a used window is opened"
				}
			}
		},
		"views": {
			"workspaceWizard": [
				{
					"id": "workspaceWizard",
					"name": "Workspaces"
				}
			]
		},
		"viewsContainers": {
			"activitybar": [
				{
					"id": "workspaceWizard",
					"title": "Workspaces",
					"icon": "$(folder-library)"
				}
			]
		},
		"viewsWelcome": [
			{
				"id": "workspaceWizardWelcome",
				"view": "workspaceWizard",
				"contents": "Choose a folder with .code-workspace files\n[Set up](command:workspaceWizard.setup)"
			}
		]
	}
}
